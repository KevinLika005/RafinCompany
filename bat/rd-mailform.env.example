MAIL_USE_SMTP=true
MAIL_RECIPIENT=
MAIL_FROM_EMAIL=

# SMTP settings (required only when MAIL_USE_SMTP=true)
MAIL_SMTP_HOST=
MAIL_SMTP_PORT=587
MAIL_SMTP_USERNAME=
MAIL_SMTP_PASSWORD=
MAIL_SMTP_SECURE=tls

# Optional reCAPTCHA server-side verification endpoint support
RECAPTCHA_SECRET=
