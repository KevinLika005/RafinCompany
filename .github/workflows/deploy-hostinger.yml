name: Deploy to Hostinger

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: hostinger-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Upload site to Hostinger
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
    env:
      HOSTINGER_FTP_HOST: ${{ secrets.HOSTINGER_FTP_HOST }}
      HOSTINGER_FTP_USERNAME: ${{ secrets.HOSTINGER_FTP_USERNAME }}
      HOSTINGER_FTP_PASSWORD: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
      HOSTINGER_FTP_PORT: ${{ secrets.HOSTINGER_FTP_PORT }}
      HOSTINGER_FTP_SERVER_DIR: ${{ secrets.HOSTINGER_FTP_SERVER_DIR }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate deployment settings
        shell: bash
        run: |
          set -euo pipefail

          test -n "$HOSTINGER_FTP_HOST" || { echo "Missing secret: HOSTINGER_FTP_HOST"; exit 1; }
          test -n "$HOSTINGER_FTP_USERNAME" || { echo "Missing secret: HOSTINGER_FTP_USERNAME"; exit 1; }
          test -n "$HOSTINGER_FTP_PASSWORD" || { echo "Missing secret: HOSTINGER_FTP_PASSWORD"; exit 1; }

      - name: Normalize FTP settings
        shell: bash
        run: |
          echo "DEPLOY_FTP_PORT=${HOSTINGER_FTP_PORT:-21}" >> "$GITHUB_ENV"
          echo "DEPLOY_FTP_SERVER_DIR=${HOSTINGER_FTP_SERVER_DIR:-public_html/}" >> "$GITHUB_ENV"

      - name: Deploy files with FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ env.HOSTINGER_FTP_HOST }}
          username: ${{ env.HOSTINGER_FTP_USERNAME }}
          password: ${{ env.HOSTINGER_FTP_PASSWORD }}
          port: ${{ env.DEPLOY_FTP_PORT }}
          protocol: ftp
          local-dir: ./
          server-dir: ${{ env.DEPLOY_FTP_SERVER_DIR }}
          state-name: .github/.ftp-deploy-sync-state.json
          log-level: standard
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/.tools/**
            **/.tmp-edge/**
            **/.vendor/**
            **/.vscode/**
            **/Thumbs.db
            **/*.docx
            **/*.log
            **/*.md
            .gitattributes
            .gitignore
